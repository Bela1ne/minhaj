import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryDark = Color(0xFF0B3B3C);
  static const Color accent = Color(0xFF00BFA6);
  static const Color surface = Color(0xFF102F2F);
  static const Color background = Color(0xFF0D2828);
  static const Color card = Color(0xFF134141);
  static const Color textLight = Colors.white;
  static const Color textMuted = Colors.white70;
}
